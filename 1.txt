clear

命令
/usr/bin/env
环境变量
aaa=1234
bbb=5678
cccc=1234
每个字符串的格式为name=value，并以“”“零字节结尾”“”“”
要编写这样的外壳代码，我们需要在堆栈上构造一个环境变量数组，并在调用exeve（）之前将该数组的地址存储到edx寄存器中。在堆栈上构造此数组的方式与构造argv[]数组的方式完全相同。基本上，我们首先在堆栈上存储实际的环境变量字符串。每个字符串的格式为name=value，并以零字节结尾。我们需要得到这些字符串的地址。然后，我们在堆栈上构造环境变量数组，并将字符串的地址存储在该数组中。数组应如下所示（元素0、1和2的顺序无关紧要）：